# Dev-only: point the frontend at the local Akort API (proxied by Vite).
# Production builds (netlify) don't load this file, so they fall back to the
# built-in demo data provider automatically.
VITE_API_URL=/api
